{
  "name": "my-portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-certs.cjs && vite",
    "build": "node scripts/build-certs.cjs && vite build",
    "preview": "node scripts/build-certs.cjs && vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
